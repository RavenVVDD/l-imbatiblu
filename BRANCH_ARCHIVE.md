# Branch Archive

This repo has a few historical feature branches that are no longer the active line of work.

## Active line

- `dev/gabriel` -> `892ef94` (`Refactor screen management and update navigation logic`)

## Archived branches

- `feat/host-show-wheel-sync` -> `9a3ee30` (`Unifica host/show y suma ruleta al flujo en vivo`)
- `feat/panels` -> `d10d95b` (`feat panels`)
- `cleanup-original` -> `7be72fe` (`Update quiz show assets and gameplay`)
- `quiz-show-gameplay` -> `7be72fe` (`Update quiz show assets and gameplay`)

## Notes

- `cleanup` is the current upstream branch for the refactor work and already includes the latest `dev/gabriel` history.
- The archived branches are kept only as references for historical context and should not be used as the base for new work.
